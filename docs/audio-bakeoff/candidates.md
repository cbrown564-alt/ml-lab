# Voice bake-off — finalist slate (Milestone #3)

**Status:** Generated 2026-06-27 · Supports [audio-narration-bakeoff-plan.md](../audio-narration-bakeoff-plan.md) §6

All six read the frozen [corpus](corpus.md) (Sections A, B, probe). Clips +
blind contact sheet are generated by `scripts/bakeoff-generate.ts` into
`docs/audio-bakeoff/clips/` and `contact-sheet.html` (both gitignored — local
listening artifacts). Open `contact-sheet.html` in a browser to score blind;
codes are stable across sections, display order shuffled per section.

| Code | Provider | Voice | Model | Settings | Why it's here |
|---|---|---|---|---|---|
| **c1** | ElevenLabs | Roger (`voices[0]`) | `eleven_multilingual_v2` | **default** | The **baseline** — today's unintended stock floor to beat. |
| **c2** | ElevenLabs | George — Warm, Captivating Storyteller (British) | `eleven_multilingual_v2` | tuned\* | The **intended** narrator (`AUDIO_VOICE="George"`), never auditioned because of the name-match bug. |
| **c3** | ElevenLabs | Daniel — Steady Broadcaster (British) | `eleven_multilingual_v2` | tuned\* | Documentary-broadcast register; the steadiest reader. |
| **c4** | ElevenLabs | River — Relaxed, Neutral, Informative | `eleven_multilingual_v2` | tuned\* | Calm/neutral; a quieter register option. |
| **c5** | Gemini | Charon (Informative) | `gemini-3.1-flash-tts-preview` | doc-style steer† | Gemini's informative/deep voice — the expressiveness hypothesis. |
| **c6** | Gemini | Sulafat (Warm) | `gemini-3.1-flash-tts-preview` | doc-style steer† | Gemini's warm voice — expressive but warmer. |

\* **tuned preset:** `stability 0.5 · similarity_boost 0.8 · style 0.0 ·
use_speaker_boost true` — one considered "calm documentary" setting (the fair
tuning the baseline never got). Not a combinatorial sweep (plan §6).

† **doc-style steer:** the text is prefixed with a documentary-narrator style
directive ("calm, warm, unhurried … 3Blue1Brown / Distill register, never
theatrical"). Verified the directive is **not spoken aloud** (`leak=false`) and
Whisper recovery covers 95–100% of tokens, so the timing path stays viable.

## Notes carried from generation

- **George resolves now** via a substring fallback in the generator
  (`name === match || name.includes(match)`). The production `resolveVoiceId`
  still does exact-match only — fold the same fix in with the §7 pipeline work
  once the winner is chosen.
- **Gemini 3.1 TTS preview works** with no fallback to 2.5 needed.
- Per-clip durations / recovery coverage are in `bakeoff-manifest.json`.

## Open follow-ups for the slate

- If the listening test is close between providers, the plan's tie-break (§6.3)
  is a 4th, longer section.
- Newer ElevenLabs models (`eleven_turbo_v2_5` / `v3`) were **not** included —
  kept to `multilingual_v2` so timing stays confirmed and voice is the only
  variable. Worth a follow-up only if EL wins and we want more expressiveness.
