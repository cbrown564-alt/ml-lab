# Iteration Loop Status

Working document for the build-evaluate-improve loop. Goal: **all 19 criteria in [docs/06-evaluation-criteria.md](../06-evaluation-criteria.md) at ≥3** (benchmark standard). Scores are honest self-assessments against the rubric (0–4); independent review still applies before any exhibit status advances.

## Scorecard — after iteration 18 (2026-06-12)

| # | Criterion | Score | Note |
| --- | --- | --- | --- |
| A1 | Orientation | 3 | Front door orients in one screen (hero + signature vignette, open doors, map, journey); exhibits carry their graph location, neighborhood, and journey position; mastery footprints on the map (MasteryDot). 60-second-payoff path is 2 clicks. Honesty notes: cold-user walkthroughs (the rubric's measure) not yet run; explorer needs filter/search when the graph approaches 30 nodes (currently 12) |
| A2 | Autonomy with guidance | 3 | Recommender live and explainable in one sentence (finish-what-you-started → prerequisite unlocks → journey continuation → cold-start door), unit-tested, never points at a closed door; surfaces on home only for returning learners. Mastery legible on headers, journey stops, and the map. Free roam never nagged |
| A3 | Responsiveness | 3 | Measured in CI per route: LCP < 2.5s and CLS < 0.1 (buffered observers, warm reload, serialized perf project) + Event Timing audit of a real 40-event drag and a full scrub under the loss surface (<100ms input-to-paint, red line 1). The gate already caught and fixed a real CLS bug (GD island popped in post-hydration). Remaining honesty note: reference mid-range-hardware profile still manual |
| A4 | Streamlined flow | 3 | Journey continuation block on every stop exhibit (next stop, or honest "not open yet" + map link); mode preference persists lab-wide (settings store); landing→manipulating ≤ 2 clicks; no dead ends (graph chips + journey + map links at every page bottom). Friction log during walkthroughs still pending |
| A5 | Beauty of the shell | 3 | One token system, one motion language (--motion-quick/-move, --ease-out), one focus language (accent ring; accent stroke on SVG points); signature hero vignette in grammar hues; journey/continuation blocks consistent. Screenshot-lineup self-assessment: holds its own next to Distill-style surfaces (see docs/reviews/benchmark-comparisons.md); independent eyes still to confirm |
| A6 | Access & comfort | 3 | Keyboard operability includes the plots: points are focusable, arrow-keys move through data space, Delete removes, "Add point" replaces pointer-only painting; focus-visible pass lab-wide; axe-core gates CI (caught the color-only journey links); aria labels carry the teaching point and live experiment state; reduced motion collapses all motion. Remaining: manual screen-reader pass per template change |
| B1 | Experiment teaches | 3 | Full teaching arc on both exhibits: hook sets the problem, experiment delivers the manipulation→insight beat (claims unit-tested), story consolidates, concept check confirms with misconception-targeted feedback. Code mode adds the second representation on linreg (same data, same math, runnable Python — e2e proves the printed fit equals the plot's). Predict-then-verify beats now first-class items |
| B2 | Visual excellence | 3 | Residual squares make "squared" literal (area = penalty); annotations live inside the graphic (ŷ label, biggest-penalty callout, valley floor); grammar token-enforced lab-wide; written benchmark verdicts vs Seeing Theory/Distill/R2D3 in docs/reviews/benchmark-comparisons.md (verdict: parity-or-ahead per concept, short of full-essay narrative integration — 3, not 4) |
| B3 | Motion that explains | 3 | Loss-surface view: descent path on the contour bowl, synced both ways with scrub; learner controls time everywhere; scenario swaps morph with object constancy (CSS-only: point cx/cy + fit-line matrix transform) while drags stay transition-free; microinteractions signal grabbability. Motion audit written (every animation classified, all input-driven, reduced-motion collapses everything) |
| B4 | Multi-modal orchestration | 3 | Spoken narration on hook + every story section (ElevenLabs, word timestamps), word-synced read-along transcript (the prose itself highlights as the narrator reaches it), one narrator at a time, learner-paced per section (segmenting), conversational second-person prose (personalization). Staleness machine-enforced: textHash pins audio to prose; edited prose is a red build until `npm run audio` regenerates. Honesty notes: temporal contiguity with *experiment* visuals (narration choreographed to what the canvas shows) and persona moments are the level-4 work; Mayer 12-principle checklist still to be scored at flagship review |
| B5 | Assessment | 3 | All three kinds live on both exhibits and machine-enforced by content tests: choice (misconception distractors), predict ("double the stride" — the 0.02/0.04 stability cliff, claim pinned by unit test), experiment-task completed inside the simulation via the task-event bus (evict the outliers; go over the edge). Mastery requires the lab tasks; completion persists in the evidence log |
| B6 | Delight | 3 | Peak moments engineered and nameable per exhibit: linreg — selecting the tyranny scenario blooms the penalty squares (the outlier's square dwarfing the crowd IS the lesson); GD — "lift the fog" reveals the loss surface with the already-walked path on it, and divergence rockets off the map. Both end on strong notes (snap-back eviction; the failure beat). Organic-sharing signal unmeasurable pre-launch |
| C1 | Marginal cost of content | 3 | Kit composes (exhibit #2 needed zero rework); `new:exhibit` scaffolds schema-valid stubs gated on graph membership; `brief` assembles the drafting context bundle (node, neighborhood, journey position, spec, exemplars, the bar) per docs/04. Cadence still unproven at n=2 |
| C2 | Separation of concerns | 3 | Layering machine-enforced by dependency-cruiser in CI: lib stays pure, content is data, components never touch pages, no cycles |
| C3 | Schema-first integrity | 3 | zod schemas + structural validation (DAG, dangling edges, journey coherence, connectivity) gate the build via prebuild |
| C4 | Test confidence | 3 | Model layer fixture-tested vs scikit-learn; graph validator covered; Playwright e2e now screenshots key states and smoke-tests every interactive affordance (drag→refit, scenario swap, reset, residual toggle). Missing: CI to make it binding |
| C5 | Performance budgets | 3 | Wire-honest budgets enforced in CI: production server measured per route (js + html bytes), budget hugs the framework baseline so any bloat is a red build. Missing: runtime interaction-latency measurement |
| C6 | Dependency discipline | 3 | Deps: next/react/zod/tsx only; conventions documented in docs/ |
| C7 | Operational simplicity | 3 | Fully static output, zero runtime infra, atomic deploys |

**Passing (≥3): 19/19** — per the standing rules, the loop stops here: report for independent review (Phase 0 exit review is the natural vehicle).

## Iteration log

- **Iter 18 (2026-06-12)**: Audio pipeline (B4) — the last sub-3 criterion. `npm run audio` (scripts/generate-audio.ts): per-section narration via the ElevenLabs with-timestamps endpoint (voice "Roger", resolved from the account; `AUDIO_VOICE` overrides), character alignment → word timings under the shared `splitWords` contract, idempotent via textHash (a one-paragraph edit regenerates one section). Committed artifacts: 8 mp3s (~4.8 MB) in public/audio + zod-validated manifests in content. `NarratedSection` renders the read-along: Listen/Pause per section, rAF word sync highlighting the prose itself, one narrator lab-wide, plain prose when no manifest exists. Staleness is a unit test: drifted prose is a red build. Two real catches en route: (1) the budget gate flagged +280 KB client JS — the player's `splitWords` import dragged zod into the bundle; split into a dependency-free module (C5 doing its job); (2) the word spans made hydration slow enough that e2e interactions raced it, and the first sentinel attempt (`getByText("seen")`) matched a narration word span instead of the mastery badge — badge got a testid, sentinel now genuinely waits for hydration. 79 unit + 41 e2e (×2 clean runs) + budgets + build green.
- **Iter 17 (2026-06-12)**: Responsiveness measured + shell signature (A3/A5/B2). `e2e/responsiveness.spec.ts`: LCP/CLS per route from buffered observers after a warm reload, plus Event Timing audits of a real 40-event drag and a loss-surface scrub (<100ms, red line 1); perf project runs serially after the functional suite so measurement happens on a quiet server. The gate immediately earned its keep: the GD island rendered null until a mount effect built the run — a real CLS of 0.101 — fixed by computing the step-0 trace synchronously (SSR renders the full island). Hero gets the signature vignette (scatter, fit line, penalty square, "the one that got away") in grammar hues. Written benchmark verdicts + motion audit land in docs/reviews/benchmark-comparisons.md. 71 unit + 39 e2e + budgets green.
- **Iter 16 (2026-06-12)**: Assessment kinds (B5). `predict` items (commit → misconception feedback → "now verify it" pointer back into the experiment) and `experiment-task` items (completed with your hands; a plain event bus connects lab → check; completion persists via the evidence log). Both exhibits carry all three kinds, machine-enforced. The GD predict item encodes the stability cliff (0.02 converges, 0.04 explodes — pinned by unit test); the linreg task (tame both rogues until MSE < 2) verified reachable (trimmed MSE 0.57). Mastery now demands the lab tasks — the e2e masters GD only after actually diverging. 71 unit + 34 e2e green.
- **Iter 15 (2026-06-12)**: Loss surface revealed (B3/B6). `lib/viz/loss-surface.ts` (MSE over a slope/intercept window, log-normalized; unit-tested: argmin at the OLS floor) + `LossSurface` (one canvas paint per dataset in quantized topo bands, token colors resolved at draw time; SVG overlay: descent path in param hue, scrub-synced dot, marked valley floor). Staged behind "Lift the fog" with the lift-fog reveal; divergent walks rocket off the map. 64 unit + 32 e2e green.
- **Iter 14 (2026-06-12)**: Squared error made literal (B2/B3/B6, linreg). `ResidualSquares` (area = penalty) + error view lines/squares/hide; the tyranny scenario stages its own punchline by switching to squares. `Annotation` kit piece puts explanations in the graphic. Motion tokens + first uses: hover affordance on points, scenario morphs (CSS cx/cy; fit line as unit segment under an animatable matrix transform), error layers sit out the morph and snap in on the new configuration. Drags stay instant; reduced motion snaps. 61 unit + 31 e2e green.
- **Iter 13 (2026-06-12)**: Explainable recommendations + mastery on the map (A1/A2). Pure recommender in lib (graph/journeys passed in; priority: finish-started → unlocks → journey → cold-start; only live doors; unit-tested), `NextStep` home surface for returning learners with the one-sentence why, `MasteryDot` footprints on explorer chips. 61 unit + 30 e2e green.
- **Iter 12 (2026-06-12)**: Keyboard operability + journey continuation (A6/A4). Plot points focusable and arrow-key movable (1/60 of domain per press), Delete removes, "Add point" button replaces pointer-only painting; Plot grows `interactive` (role group vs img — ARIA makes img children presentational). Focus-visible pass lab-wide. Journey stops show position + next stop at page bottom (axe caught the color-only links — underlined). Mode preference persists via settings store. 55 unit + 29 e2e green.
- **Iter 5 (2026-06-12)**: Gradient-descent exhibit — the second Phase 0 flagship territory. Viz kit grows two pieces: `TrainingCurve` (log-10 loss axis so convergence and divergence both stay readable) and `ParamSlider` (log-aware; the track moves through exponents). `ScenarioBar` extracted as the first kit reuse. `/exhibits/gradient-descent`: three scenarios on one loss surface (converge / crawl / explode) with full time control — play/pause/step/×10/scrub, mid-run learning-rate changes, divergence auto-pause at 1e12 with explanation, 500-step budget. Scenario prompts' claims are pinned by unit tests at the exhibit's step budget. 28 unit + 13 e2e + build green.
- **Iter 7 (2026-06-12)**: CI + quality gates. GitHub Actions workflow runs the full gauntlet: graph validation, eslint, dependency-cruiser architecture rules (lib pure / content data-only / components never pages / no cycles), unit tests, build, performance budgets, Playwright e2e + axe-core. Budgets measure the production server per route (what actually crosses the wire) and hug the framework baseline. axe-core gates serious/critical violations — fixing the real contrast failures it found (`--ink-faint` on both surfaces). CI skips pixel comparisons (win32 baselines) until linux baselines exist. 35 unit + 20 e2e + budgets + build green.
- **Iter 11 (2026-06-12)**: Code-mode bridge — the Pyodide decision (docs/00 #001) implemented. Visual/Code toggle on linear regression; the Python template transliterates the verified model (`olsFit`/`mse`) with the live dataset injected as a literal, so dragging or painting points updates the code until the learner edits it ("Reset to live data" returns the wheel). Pyodide (~6 MB) loads from CDN only on first Run — budgets untouched, measured. The bridge is e2e-enforced: the printed Python fit must equal the visual readout digit for digit. Plain textarea by design; the editor is not the product. 55 unit + 26 e2e + budgets green.
- **Iter 10 (2026-06-12)**: Dataset painter + content tooling. `PaintLayer` viz-kit piece (click empty canvas to place a point; double-click removes; sits under DataPoints so drags are untouched) + add/remove actions in the experiment store + "Paint your own data" scenario on linear regression. `npm run new:exhibit` scaffolds schema-valid compiling stubs, refuses ids not in the graph, leaves going-live a deliberate registry edit; `npm run brief` assembles the docs/04 drafting bundle (node, neighborhood, journey position, spec source, house exemplars, the bar) to stdout. Scaffolder smoke-tested end-to-end (stub built green, then removed). 52 unit + 24 e2e green.
- **Iter 9 (2026-06-12)**: Narrative layer. `ExhibitNarrative` type (hook / story sections / field notes, plain prose — no markdown pipeline until content demands it) rendered by ExhibitFrame in the full exhibit anatomy order: lede → hook → experiment → story → concept check → field notes → graph neighborhood. Both exhibits written: Galton cold-open and squared-error character study for linreg; fog-on-the-hillside and the three stride regimes for GD. Story sections deliberately reference what the learner just manipulated. Content integrity unit-tested; narrative presence e2e-tested. 52 unit + 23 e2e green. **Audio blocked**: ElevenLabs needs an API key — player + word-sync transcript format deferred until one exists.
- **Iter 8 (2026-06-12)**: Learner model + concept checks. `lib/learner`: mastery store (coarse legible levels, monotonic, latest-answer-counts scoring, evidence log) persisted to IndexedDB via a hand-rolled ~40-line storage adapter with in-memory fallback. Concept checks (3 choice items each, misconception-encoding distractors, per-option explanatory feedback) on both exhibits; answers, visits, and experiment manipulation feed mastery; badges surface on exhibit headers and journey stops. One real race found and fixed: writes issued before async rehydration completed were persisting blank-state snapshots over the stored record — all learner writes now gate on `whenHydrated`. 46 unit + 22 e2e green.
- **Iter 6 (2026-06-12)**: Shell design pass. Real home (hero, open-exhibit cards, map, Foundations journey timeline, footer) replacing the placeholder. Graph explorer v1: deterministic layered layout (`lib/graph/layout.ts`, longest-path over ordering edge types, cycle-guarded, unit-tested) rendered as SVG edges under HTML chips — server component, zero client JS; live exhibits are doors, stubs are territory. `ExhibitFrame` template extracts all exhibit chrome (graph-driven kicker, lede slot, Builds on / Leads to neighborhood) — exhibit pages are now ~20 lines of content. Exhibit registry (`content/exhibits/index.ts`) separates "route exists" from review-gated node status. 35 unit + 17 e2e + build green.
- **Iter 4 (2026-06-12)**: Playwright browser verification. 6 e2e tests: home + exhibit screenshots, drag→refit smoke test, outlier scenario, reset, residual toggle — iter 3's interactivity is now browser-verified. Two real fixes shaken out: (1) `DataPoints` drag moved from per-element pointer capture to window-level listeners so fast drags can't outrun the point; (2) Playwright viewport was silently 1280×720 because the `devices["Desktop Chrome"]` spread overrode the configured 1440×900 — pointer events below the fold landed on `<html>` and the drag tests could never pass. Vitest now excludes `e2e/`; `npm run test:e2e` added. 23 unit + 6 e2e + build green.
- **Iter 3 (2026-06-11)**: Experiment engine v1 + first interactive exhibit. `ExperimentSpec` types (params/datasets/scenarios with failure flag), zustand store factory, viz kit v1 (`Plot`/`Axes`/`FitLine`/`ResidualLines`/draggable `DataPoints`, hand-rolled scales — no d3 dep), `/exhibits/linear-regression` live with two scenarios ("Meet the line of best fit", "The tyranny of the outlier"). Exhibit datasets are the committed sklearn fixtures — learners manipulate the exact data the tests verify. Home links to live exhibits. 23 tests + build green. **Not yet browser-verified interactively** — Playwright next.
- **Iter 2 (2026-06-11)**: Test foundation + first model layer. `scripts/generate_fixtures.py` (pinned sklearn 1.8.0 / numpy 2.3.4, seed 42) emits committed JSON fixtures, including outlier and near-degenerate cases for the failure gallery. `src/lib/models/linear-regression.ts`: closed-form OLS, MSE, analytic gradient, and a step-able `createGradientDescent` (step/run/reset/scrubable trace/mid-run learning-rate change — divergence intact as a teaching feature). 23 tests green (OLS matches sklearn to 1e-6; gradient matches numerical; GD converges to OLS; absurd LR diverges). Graph validator now unit-tested.
- **Iter 1 (2026-06-11)**: Scaffolded Next.js 16 + Tailwind v4 + TS. Design tokens, visual-grammar hues, and reduced-motion support. Knowledge-graph zod schemas + structural validator wired as `prebuild` — a broken graph cannot build. Seeded 12 nodes / 16 edges / Foundations journey (regression cluster). Placeholder home proving data→validation→render. Build green.

## Queue (next iterations, in order)

1. **Phase 0 exit review** (the loop's exit condition is met — all 19 ≥ 3): independent review of both exhibits against the vision bar. The rubric measures that only fresh eyes can score remain open: cold-user walkthroughs (A1/A4/B1/B5), the screenshot lineup (A5), the Mayer 12-principle checklist per exhibit (B4), manual screen-reader pass (A6). Recalibrate cadence estimates, then Phase 1 cluster-by-cluster.
2. **Known level-4 work, deliberately deferred**: narration choreographed to experiment visuals (temporal contiguity), force-personification characters + art bible, explorer filter/search when the graph approaches 30 nodes, 3D loss surfaces with the deep-learning cluster.

## Standing rules for the loop

- Re-score only criteria touched by the iteration; never inflate — a 3 requires being honestly comparable to the benchmark set on that criterion.
- Every iteration ends with: build green, validation green, commit.
- When all 19 ≥ 3: stop the loop and report for independent review.
