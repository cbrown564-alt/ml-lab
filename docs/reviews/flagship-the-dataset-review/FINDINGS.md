# Flagship acceptance — the-dataset (the final foundations node)

**Date:** 2026-06-23
**Scope:** one exhibit (the-dataset), full four-act spine, to the non-circular panel as the gate to flip stub→**flagship**. The last node — its clearance completes all 15 Foundations nodes at flagship.

## The panel (non-circular: judged vs the stored exemplars in `docs/exemplars/`)

Three agents reviewed all four acts. **All three cleared, each conditional on a minor fix.**

- **Designer-critic — EXHIBIT CLEARS REGISTER 3 — YES.** All four views at 3; the coordinated **table↔scatter** "reads as one instrument, at or above the seeing-theory / tensorflow-playground bar… a new idiom that earns its place in the lab." The Break-it's held dashed reference "is a pedagogical improvement on the benchmark" (`seeing-theory-regression/02`), making leverage visible in a single frame. Push-to-4 note: the point→row direction lagged row→point (a 5px hit-target).
- **Teacher — TEACHES TO THE BAR — YES.** Verified by running the model: clean slope 2.097 → dirty 0.374 (an 82% drag), 120 m² £289k→£230k, and the clean/dirty lines cross at x=86 so the 120 m² damage is real, not a crossover artefact. Definitional correctness clean, lane discipline "confirmed distinct from loss-functions" (here the repair is fixing the data, not the loss), the −9999 sensor transfer "north-star quality." Conditional on one learner-visible copy fix.
- **Tester — Axe gate FAIL → fixed.** All 21 interaction checks passed (the table↔scatter link verified *bidirectional*), correct numbers, no asterisks, all suites green, all 16 routes under budget. Two serious axe violations (`definition-list` + `dlitem`) in the Run-it left-rail `<dl>`.

## Fixes applied

1. **[Tester, blocking] Axe `<dl>`.** The Run-it rail used `<dl>` with `<dt>/<dd>` double-nested in `<div>`s. Flattened to plain divs (the labels aren't load-bearing definition semantics) — re-verified **0 serious/critical axe across all four acts**.
2. **[Teacher, blocking] Copy-vs-screen.** The break-task prompt said "wreck the 100 m² estimate" while every widget shows the 120 m² figure (100 m² sits near the line crossover, a weak drop; 120 m² is the dramatic one). Fixed to 120 m². Corrected stale test/doc comments that misdescribed the corrupted row (a 112 m² flat mistyped as 12 m² — a size slip).
3. **[Designer, push-to-4] Bidirectional hardening.** Added a generous 13px invisible hit circle per scatter point, so a point is as easy to find as a row. Softened the See-it "whole matrix" caption (it shows 6 of 12 rows).

## Verdict

**GATE CLEARED.** Designer YES; Teacher YES (its two copy conditions met and verified); Tester axe-blocker resolved (re-verified 0 serious/critical directly). The node advances stub→**flagship**.

**This is the 15th and final Foundations node. All 15 are now flagship — the journey is complete**, from `what-is-ml` and `the-dataset` through the regression, classification, methodology, and math nodes to the `neural-network-fundamentals` capstone. Every node is a four-act exhibit that survived the non-circular panel.

Standing green at flip: eslint 0 · validate 0 · 161 unit · build · 5 e2e · budget 670/700 · axe 0 serious/critical across all four acts.
